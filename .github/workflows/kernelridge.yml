name: MLOps Assignment Workflow

on:
  push:
    branches:
      - kernelridge

jobs:
  train-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.14]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Upgrade pip
        run: |
          python -m pip install --upgrade pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Decision Tree Model
        run: |
          echo "===== Decision Tree Model ====="
          python train.py

      - name: Run Kernel Ridge Model
        run: |
          echo "===== Kernel Ridge Model ====="
          python train2.py
      - name: Run test for Decision Tree Model
        run: |
          echo "===== Test Decision Tree Model ====="
          python test_model_DT.py
      - name: Run test for Kernel Ridge Model
        run: |
          echo "===== Test Kernel Ridge Model ====="
          python test_model_KR.py
